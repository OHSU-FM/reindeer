require 'spec_helper'

RSpec.describe Message, type: :model do
  # TODO once we figure out if archive is a thing we want to do
  # it "has a factory" do
  #   expect(build :message).to be_valid
  # end
  #
  # context "methods" do
  #   it "can read and write archived" do
  #     m = create :message
  #     expect(message.archived?).to be_false
  #
  #     m.archive!
  #     expect(message.archived?).to be_true
  #   end
  # end
end
