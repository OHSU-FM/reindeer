<h1>this the index</h1>

<div class="rooms">
  <% @rooms.each do |room| %>
    <div class="room">
      <%= link_to room.identifier, room_path(room), class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
