$('#oasis-graphs2').remove();
$('#advisor-reports').html("<%= j render partial: 'coaching/students/advisor_reports', locals: { meetings: @meetings, code: @code } %>");
$('#IndividualAdvisorRpt').dataTable( {
    language: {
        searchPlaceholder: "Student LastName, FirstName",
        "emptyTable": "No Record Found!"

    }
} );
