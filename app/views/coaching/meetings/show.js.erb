$('#meetings-table').append("<%= j render partial: 'meeting_row', locals: { meeting: @meeting }%>");
$('#newMeetingModal').modal_success();
// has to do a browser reload , otherwise, it will display the meeting_form esp. added new goal
location.reload()  // to reload detail meeting records, otherwise, the meeting form would not initialize after submit
$('.modal-meeting').html("<%= j render partial: 'coaching/meetings/meeting_form', locals: { student: @meeting.user, advisor_type: @advisor_type, advisor_id: @advisor_id } %>");
