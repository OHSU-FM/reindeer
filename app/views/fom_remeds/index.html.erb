<h1>Fom Remeds</h1>

<table class="responsive nowrap table table-hover" id="dttb-fom_remeds" data-sort="true" data-source="<%= url_for(format: :json)%>">
  <thead>
    <tr>
      <th data-data="user_id">User</th>
      <th data-data="block">Block</th>
      <th data-data="url" data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export" width="100px"></th>
    </tr>
  </thead>

</table>

<br>

<%= link_to 'New Fom Remed', new_fom_remed_path %>